<%- model_class = Store -%>
<div class="page-header">
  <h1><%= @store.name %></h1>
</div>
<%= image_tag @store.logo.url(:medium) %>
<p><%= @store.address %></p>
<p>Status: <%= @store.store_status_id %></p>
<div class="page-header">
  <h1>Categorías en las que participa</h1>
</div>
<p>Las categorías se agregan automáticamente en función de las categorías de los productos que se seleccionen</p>
<div class="page-header">
  <h1>Productos</h1>
  <%= link_to 'Nuevo Producto', new_store_product_path(@store) %>
</div>
<div class="form-actions">
  <%= link_to t('.back', :default => t("helpers.links.back")),
              stores_path, :class => 'btn'  %>
  <%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_store_path(@store), :class => 'btn' %>
  <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              store_path(@store),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>
</div>
